export interface Slider {
  id: number;
  img: string;
  title: string;
  desc: string;
  bg: string;
}

export interface Category {
  id: number;
  img: string;
  title: string;
}

export interface Product {
  id: number;
  img: string;
}
